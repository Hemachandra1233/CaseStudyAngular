import { RoleAssignRequest } from './role-assign-request';

describe('RoleAssignRequest', () => {
  it('should create an instance', () => {
    expect(new RoleAssignRequest()).toBeTruthy();
  });
});
